
@Library('dst-shared@master') _

dockerBuildPipeline {
    repository = "cray"
    imagePrefix = "cray"
    app = "cfs"
    name = "cray-cfs"
    description = "Cray management system configuration framework service"
    product = "csm"
}
